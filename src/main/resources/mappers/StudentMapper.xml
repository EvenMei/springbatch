<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.terminus.mapper.StudentMapper">

    <select id="findList" resultType="com.terminus.pojo.Student">
        select * from student
    </select>

    <insert id="addStudent" useGeneratedKeys="true" keyProperty="id" keyColumn="id" >
        insert into student values(null,#{name},#{age},#{address});
    </insert>

    <insert id="addStudent2" >
        insert into student values(null,#{name},#{age},#{address});
    </insert>

</mapper>