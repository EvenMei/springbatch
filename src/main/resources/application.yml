spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test4mysql
    username: root
    password: root
    schema: classpath:/org/springframework/batch/core/schema-mysql.sql
  batch:
    initialize-schema: always
#    job:
#      names: parentJob-01
#      names: myjob-02
###mybatis 配置项
mybatis:
  mapper-locations: mappers/*.xml
  type-aliases-package: com.terminus.pojo